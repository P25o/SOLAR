/* ============================================================
   แพ็กเกจที่ขาย — แก้ราคา/เวลาที่ไฟล์นี้ที่เดียว
   ทั้งเซิร์ฟเวอร์และหน้าเว็บจะดึงไปใช้ตรงกันเสมอ

   amountSatang = ราคาเป็น "สตางค์" (Stripe คิดหน่วยเล็กสุดของสกุลเงิน)
                  1000 สตางค์ = 10 บาท
   ⚠️ Stripe + PromptPay รับขั้นต่ำ 10 บาท ห้ามตั้งต่ำกว่านี้
   ============================================================ */

const PACKAGES = [
  {
    id: 'p10',
    name: 'แพ็กเริ่มต้น',
    amountSatang: 1000,   // 10 บาท
    minutes: 15,
    note: 'ชาร์จแก้ขัด'
  },
  {
    id: 'p20',
    name: 'แพ็กคุ้มค่า',
    amountSatang: 2000,   // 20 บาท
    minutes: 40,
    note: 'คุ้มกว่าแพ็กแรก 33%'
  },
  {
    id: 'p50',
    name: 'แพ็กจัดเต็ม',
    amountSatang: 5000,   // 50 บาท
    minutes: 120,
    note: 'คุ้มกว่าแพ็กแรก 80%'
  }
];

function findPackage(id) {
  return PACKAGES.find(p => p.id === id) || null;
}

module.exports = { PACKAGES, findPackage };
